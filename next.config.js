/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.istockphoto.com', 'programandovalores.files.wordpress.com','img.freepik.com'],
      },
}

module.exports = nextConfig
